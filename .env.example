# Neon PostgreSQL connection string for live dashboard metrics.
# Copy this file to .env and fill in the URL.
# The dashboard generator will query Neon directly when this is set,
# falling back to INDEX.yaml when it's not.
NEON_DATABASE_URL=postgresql://user:password@ep-XXXXX.region.aws.neon.tech/dbname?sslmode=require
