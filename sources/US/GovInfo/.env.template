# No API key required for GovInfo bulk data
# The bulk data repository at https://www.govinfo.gov/bulkdata is freely accessible

# Optional: For API access (not needed for bulk data)
# GOVINFO_API_KEY=your_api_key_here
