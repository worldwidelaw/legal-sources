# LegiScan API key — free tier, 30,000 queries/month.
# Register at: https://legiscan.com/legiscan-register
# After signup, you'll receive an API key by email.
#
# Free tier covers:
#   - All 50 US states + DC + US Congress
#   - getBill, getBillText (PDF + HTML), getAmendment, getRollCall,
#     getMasterList, getSessionList, getDataset (bulk session download)
#   - Historical sessions (not just current)
#
# ToS note: Free tier is intended for non-commercial / public-benefit use.
# Commercial use requires a paid GAITS subscription.
LEGISCAN_API_KEY=
