# US/CourtListenerBulk requires NO credentials.
#
# The CourtListener bulk-data S3 bucket (com-courtlistener-storage) is public.
# boto3 is configured with botocore.UNSIGNED so anonymous GET works.
#
# This file exists only to keep the standard source-folder layout. Leave empty.
