# NZ/Legislation does not require authentication.
# XML files are publicly accessible without an API key.
#
# If you want to use the official API instead (for higher rate limits),
# you can request an API key from contact@pco.govt.nz and add it here:
#
# NZ_LEGISLATION_API_KEY=your_api_key_here
