fetch:
  max_retries: 3
  rate_limit: 2.0
  timeout: 90
schema:
  key_fields:
  - name: _id
    required: true
    type: string
  - name: title
    required: true
    type: string
  - name: date
    required: false
    type: date
  - name: content_type
    required: false
    type: string
source:
  auth: none
  country: INTL
  data_types:
  - doctrine
  description: 'Policy documents, standards, reports, and consultations from the Financial Stability Board (FSB). ~930 publications fetched via sitemap discovery and HTML scraping with PDF full-text extraction.

    '
  id: INTL/FSB-Publications
  name: Financial Stability Board Standards and Reports
  url: https://www.fsb.org/publications/
