# DK/DTIL -- Danish Data Protection Authority (Datatilsynet)
# GDPR enforcement decisions from the Danish DPA

source:
  id: "DK/DTIL"
  name: "Danish Data Protection Authority (Datatilsynet)"
  country: "DK"
  url: "https://www.datatilsynet.dk"
  data_types:
    - doctrine
  auth:
    type: none

# Discovery strategy:
# 1. Parse sitemap at /sitemap.xml for decision URLs
# 2. Filter URLs matching
#    /afgoerelser/{afgoerelser|historiske-afgoerelser|tilladelser}/YYYY/mon/slug
# 3. Fetch each HTML page and extract full text from div.news-page
#    (p.lead + div.rich-text)

data_model:
  update_strategy: upsert
  dedup_key:
    - _id

rate_limit:
  requests_per_second: 1
  burst: 3
  max_workers: 3

schema:
  key_fields:
    - name: title
      type: string
      required: true
    - name: text
      type: string
      required: true
    - name: date
      type: date
      required: false
    - name: case_number
      type: string
      required: false
    - name: section
      type: string
      required: true
    - name: url
      type: string
      required: true
