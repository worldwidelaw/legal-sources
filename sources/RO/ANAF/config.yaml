schema:
  key_fields:
  - name: _id
    required: true
    type: string
  - name: title
    required: true
    type: string
  - name: text
    required: true
    type: string
  - name: date
    required: false
    type: date
  - name: url
    required: true
    type: string
  - name: doc_type
    required: false
    type: string
source:
  auth: none
  country: RO
  data_types:
  - doctrine
  description: 'Tax doctrine from the Romanian National Tax Administration Agency (ANAF). Covers fiscal bulletins (noutati legislative), ANAF orders (OPANAF), and circulars. PDFs downloaded from static.anaf.ro,
    text extracted via pypdf. ~300+ documents with full text.

    '
  id: RO/ANAF
  name: Romanian Tax Authority (ANAF)
  schedule: weekly
  url: https://www.anaf.ro
